const [count, setCount] = useState(0)
const [restID, setID] = useState('')

let incrementer = (e) => {
	setCount(count + 1)
	console.log(e.currentTarget.value)
}

let decrementer = (e) => {
	setCount(count - 1)
	console.log(e.currentTarget.value)
}